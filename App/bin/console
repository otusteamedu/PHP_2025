#!/usr/bin/env php
<?php
//chmod +x bin/console

require __DIR__ . '/../../vendor/autoload.php';

use App\Command\Storage\StorageAddTestDataCommand;
use App\Command\Storage\StorageSearchCommand;
use Symfony\Component\Console\Application;

$app = new Application();

$app->add(new StorageAddTestDataCommand());
$app->add(new StorageSearchCommand());

$app->run();
