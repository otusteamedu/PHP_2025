# Отчет по анализу и оптимизации запросов (PostgreSQL)

## 1. Состав результата

1. Скрипт создания БД: `films_DDL.sql`
2. Скрипт заполнения БД тестовыми данными (10k): `DML_10000.sql`
3. Скрипт заполнения БД тестовыми данными (10M): `DML_10000000.sql`
4. Список 6 основных запросов: `queries_6_main.sql`
5. Планы выполнения для базы до 10k строк: `EXPLAIN_10000.sql`
6. Планы выполнения для базы до 10M строк: `EXPLAIN_10000000.sql`
7. Скрипт оптимизаций: `optimizations.sql`
8. Планы после оптимизаций (10M): `EXPLAIN_10000000_optimized.sql`
9. Метрики по объектам БД и индексам: `db_metrics.sql`


## 2. 6 основных запросов

Требование по сложности соблюдено:
- Простые (1 таблица): Q1, Q2, Q6
- Сложные (join/агрегации): Q3, Q4, Q5

Все запросы вынесены в файл `queries_6_main.sql`.


## 3. Порядок выполнения

### Вариант A: анализ на 10k

1. Выполнить `films_DDL.sql`
2. Выполнить `DML_10000.sql`
3. Выполнить `EXPLAIN_10000.sql`

### Вариант B: анализ на 10M

1. Выполнить `films_DDL.sql`
2. Выполнить `DML_10000000.sql`
3. Выполнить `EXPLAIN_10000000.sql`

### Вариант C: анализ после оптимизации (10M)

1. Выполнить `films_DDL.sql`
2. Выполнить `DML_10000000.sql`
3. Выполнить `optimizations.sql`
4. Выполнить `EXPLAIN_10000000_optimized.sql`

### Размеры объектов и использование индексов

1. Выполнить `db_metrics.sql`
2. Сохранить результаты:
   - Топ-15 самых больших объектов БД
   - Топ-5 самых часто используемых индексов
   - Топ-5 самых редко используемых индексов


## 4. Таблица результатов по 6 запросам

Заполните таблицу ниже фактическими планами (`EXPLAIN ANALYZE`) и наблюдениями.

| № | Запрос | План на БД до 10 000 строк | План на БД до 10 000 000 строк | План на БД 10 000 000 после оптимизаций | Перечень оптимизаций с пояснениями |
|---|---|---|---|---|---|
| Q1 | Выбор всех фильмов на сегодня | Вставить вывод из `EXPLAIN_10000.sql` | Вставить вывод из `EXPLAIN_10000000.sql` | Вставить вывод из `EXPLAIN_10000000_optimized.sql` | Переписан фильтр по дате в диапазон (`>= CURRENT_DATE` и `< CURRENT_DATE + 1 day`) + индекс `idx_screenings_start_time_hall_movie` |
| Q2 | Подсчет проданных билетов за неделю | Вставить вывод | Вставить вывод | Вставить вывод | Частичный индекс `idx_tickets_sold_purchase_time` для `status='sold'`, ускорение диапазонного фильтра по времени |
| Q3 | Формирование афиши на сегодня | Вставить вывод | Вставить вывод | Вставить вывод | Индекс `idx_screenings_start_time_hall_movie` снижает стоимость отбора и join |
| Q4 | Топ-3 самых прибыльных фильмов за неделю | Вставить вывод | Вставить вывод | Вставить вывод | Частичный индекс `idx_tickets_sold_screening_price` уменьшает объем сканирования по sold-билетам |
| Q5 | Схема зала (свободные/занятые места) | Вставить вывод | Вставить вывод | Вставить вывод | Частичный индекс `idx_tickets_screening_seat_occupied` ускоряет поиск занятых мест |
| Q6 | Диапазон min/max цены на сеанс | Вставить вывод | Вставить вывод | Вставить вывод | Индекс `idx_tickets_screening_price` ускоряет min/max по `screening_id` |


## 5. Что должно быть приложено к сдаче

1. `films_DDL.sql`
2. `DML_10000.sql`
3. `DML_10000000.sql`
4. `queries_6_main.sql`
5. `EXPLAIN_10000.sql`
6. `EXPLAIN_10000000.sql`
7. `optimizations.sql`
8. `EXPLAIN_10000000_optimized.sql`
9. Результат выполнения `db_metrics.sql`:
   - отсортированный список 15 самых больших объектов БД
   - отсортированный список 5 самых часто используемых индексов
   - отсортированный список 5 самых редко используемых индексов
