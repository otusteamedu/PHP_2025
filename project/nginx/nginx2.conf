upstream phpbackend {
    server php1:9000;
    server php2:9000;
}
server {
    listen 80 default;

    client_max_body_size 24M;

    access_log /app/log/application.access.log;

    root /app/home/public;
    index index.html;

    # try to serve file directly, fallback to index.php
    location / {
        try_files $uri /index.php$is_args$args;
    }

    if (!-e $request_filename) {
        rewrite ^.*$ /index.php last;
    }

    location ~ \.php$ {
        fastcgi_pass phpbackend;
        #fastcgi_pass unix:/app/socket/php-fpm.sock;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PHP_VALUE "error_log=/app/log/application_php_errors.log";
        fastcgi_param NGINX_SERVER "nginx2";
        fastcgi_buffers 16 16k;
        fastcgi_buffer_size 32k;
        include fastcgi_params;
    }
}
