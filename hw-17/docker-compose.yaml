version: '3'

services:
  app:
    build:
      context: ./docker/php
      dockerfile: Dockerfile
      args:
        WITH_XDEBUG: ${WITH_XDEBUG}
    image: hw17/app
    container_name: hw17-app
    volumes:
      - ./app:/var/www/html/app
      - ./docker/php/php-socket:/var/run/php
    networks:
      - otus-network

  nginx:
    build:
      context: ./docker/nginx
      dockerfile: Dockerfile
    image: hw17/nginx
    container_name: hw17-nginx
    volumes:
      - ./app:/var/www/html/app
      - ./docker/php/php-socket:/var/run/php
    ports:
      - "8085:80"
    networks:
      - otus-network


networks:
  otus-network:
    driver: bridge
