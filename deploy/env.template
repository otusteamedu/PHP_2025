# Auto-generated from env.template via envsubst — DO NOT EDIT

# === Приложение ===
APP_NAME=${APP_NAME}
APP_DOMAIN=${APP_DOMAIN}
APP_ENV=${APP_ENV}
APP_DEBUG=${APP_DEBUG}

# только для Dev
#WWW_ROOT_DIR=./code

# === БД Postgres ===
PG_HOST=${PG_HOST}
PG_PORT=${PG_PORT}
PG_DB=${PG_DB}
PG_USER=${PG_USER}
PG_PASSWORD=${PG_PASSWORD}

# === RabbitMQ ===
RABBITMQ_HOST=${RABBITMQ_HOST}
RABBITMQ_PORT=${RABBITMQ_PORT}
RABBITMQ_LOGIN=${RABBITMQ_LOGIN}
RABBITMQ_PASSWORD=${RABBITMQ_PASSWORD}
RABBITMQ_VHOST=${RABBITMQ_VHOST}
RABBITMQ_QUEUE_TELEGRAM_FORWARD=${RABBITMQ_QUEUE_TELEGRAM_FORWARD}
RABBITMQ_QUEUE_RAG_QUERY=${RABBITMQ_QUEUE_RAG_QUERY}
RABBITMQ_QUEUE_FALLBACK=${RABBITMQ_QUEUE_FALLBACK}

# === Max Bot ===
MAX_TOKEN=${MAX_TOKEN}
MAX_CHANNEL=${MAX_CHANNEL}
MAX_WEBHOOK_SECRET=${MAX_WEBHOOK_SECRET}

# === Telegram Bot ===
TELEGRAM_TOKEN=${TELEGRAM_TOKEN}
TELEGRAM_CHANNEL=${TELEGRAM_CHANNEL}
TELEGRAM_SECRET_TOKEN=${TELEGRAM_SECRET_TOKEN}
TELEGRAM_HTTP_PROXY=${TELEGRAM_HTTP_PROXY}
TELEGRAM_PROXY_LOGIN=${TELEGRAM_PROXY_LOGIN}
TELEGRAM_PROXY_PASSWORD=${TELEGRAM_PROXY_PASSWORD}
# режим работы с Telegram: longpoll/webhook
TELEGRAM_MODE=${TELEGRAM_MODE}

# === Логирование: DEBUG/ERROR/INFO ===
LOG_LEVEL=${LOG_LEVEL}

# === SMTP для уведомлений о предложениях ===
SMTP_HOST=${SMTP_HOST}
SMTP_PORT=${SMTP_PORT}
SMTP_USER=${SMTP_USER}
SMTP_PASSWORD=${SMTP_PASSWORD}
SMTP_FROM_EMAIL=${SMTP_FROM_EMAIL}
SMTP_FROM_NAME=${SMTP_FROM_NAME}
PROPOSAL_NOTIFY_EMAILS=${PROPOSAL_NOTIFY_EMAILS}

# === E2E-тесты ===
E2E_USE_INTERNAL_DNS=false

# === Switch-nginx - только для Prod ===
SWITCH_PORT=${SWITCH_PORT}
