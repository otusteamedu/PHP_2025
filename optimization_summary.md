# Сводный отчет по оптимизации SQL-запросов

| Название запроса                    | ~10k строк | ~10M строк    | После оптимизации | Примененные оптимизации |
|------------------------------------|-------------|---------------|-------------------|--------------------------|
| **`1. Выбор фильмов на сегодня`**   | `0.195 ms` | `12.957 ms`   | `0.637 ms`        | **Функциональный индекс по дате:** `CREATE INDEX ... ON showtime (CAST((starts_at AT TIME ZONE 'UTC') AS date))`. Устраняет полное сканирование таблицы `showtime`, позволяя мгновенно находить сеансы по дате |
| **`2. Подсчёт билетов за неделю`**  | `2.540 ms` | `1233.596 ms` | `40.376 ms`       | **Покрывающий индекс:** `CREATE INDEX ... ON ticket (sold_at, status) INCLUDE (showtime_id, price_paid)`. Позволяет выполнять `Index Only Scan`, читая все данные из индекса и не обращаясь к огромной таблице `ticket` |
| **`3. Формирование афиши`**         | `0.141 ms` | `6.940 ms`    | `0.165 ms`        | **Функциональный индекс по дате:** `CREATE INDEX ... ON showtime (CAST((starts_at AT TIME ZONE 'UTC') AS date))`. Тот же индекс, что и для Запроса №1, устраняет полное сканирование таблицы `showtime` |
| **`4. Поиск прибыльных фильмов`**   | `3.951 ms` | `1241.286 ms` | `145.813 ms`      | **Покрывающий индекс:** `CREATE INDEX ... ON ticket (sold_at, status) INCLUDE (showtime_id, price_paid)`. Устраняет необходимость обращаться к основной таблице `ticket` после поиска по индексу |
| **`5. Схема зала`**                 | `0.516 ms` | `8.050 ms`    | `2.588 ms`        | **Нет.** Оптимизация не требовалась, так как запрос изначально эффективен (фильтрация по первичному ключу) |
| **`6. Диапазон цен`**               | `0.322 ms` | `0.587 ms`    | `0.314 ms`        | **Нет.** Аналогично Запросу №5, оптимизация не требовалась из-за высокой изначальной эффективности |

